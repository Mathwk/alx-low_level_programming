#include "main.h"
/**
 * is_palindrome - string symetry checker
 * @s: input strig
 * Return: 1 if true 0 other wise
 */
int is_palindrome(char *s)
{       
        int a;
        
        a = str_count(0, s);
        if (
