#include "main.h"
/**
 * _isdigit - checks for upper case letters
 * @c: letter input
 * Return: 1 for upper case and 0 otherwise
 */
int _ist(int c)
{
	if (c >= 48 && c <= 57)
		return (1);
	return (0);
}
