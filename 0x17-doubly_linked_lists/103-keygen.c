#include <stdio.h>
#include <stdlib.h>

/**
 * main - keygen generator
 * @argc: number of the arguments
 * @argv: arguments
 * Return: 0 success
 */

int main(int argc, char **argv)
{
	if (argc == 2)
		printf("%s", argv[1]);
	return (0);
}
